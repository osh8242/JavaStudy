package kr.or.kosa;

public class Emp {
	public int empno; //속성 >> member field >> instance variable
	//instance variable은 초기화를 하지않아도 default 값이 있어서 사용가능.
	//인스턴스 변수 -> 새로운 객체로 생성될 때마다 다른 값을 갖는다.
	
	public void infoPrint() {
		int data;// local variable (함수 안에 있는 지역변수)
	}
	
	
}
